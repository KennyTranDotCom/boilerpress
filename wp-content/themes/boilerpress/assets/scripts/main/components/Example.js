class Example {
    constructor(element) {
        this.init(element);
    }

    init(element) {
        /* eslint-disable no-console */
        console.log(element);
        /* eslint-enable no-console */
    }
}

export default Example;
