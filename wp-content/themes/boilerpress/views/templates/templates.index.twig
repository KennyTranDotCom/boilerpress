{% extends 'bases.default.twig' %}

{% block content %}
    <div class="o-container">
        {% for post in posts %}
            {% include [
                'components.tease-' ~ post.post_type ~ '.twig',
                'components.tease.twig'
            ] %}
        {% endfor %}
    </div>
{% endblock %}
